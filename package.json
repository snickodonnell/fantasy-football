{
  "name": "family-fantasy-football",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first fantasy football league app for families.",
  "type": "module",
  "scripts": {
    "dev": "node --no-warnings server.js",
    "start": "node --no-warnings server.js",
    "home": "powershell -ExecutionPolicy Bypass -File scripts/start-home.ps1",
    "seed": "node --no-warnings server.js --seed",
    "check": "node --check server.js && node --check public/app.js && npm test && node scripts/check-data.mjs",
    "test": "node scripts/run-tests.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
